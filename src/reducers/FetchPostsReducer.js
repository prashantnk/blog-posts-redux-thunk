const FetchPostsReducer = (posts = [], action) => {
    // if (action.type === "FETCH_POSTS") {

    //     return posts;
    // }
    console.log(action);
    return posts;
}

export default FetchPostsReducer;